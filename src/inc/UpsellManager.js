import ManagerBase from '$Base/ManagerBase';

/**
 * Editor upsell manager.
 */
class UpsellManager extends ManagerBase {
	_initLogic() {
		// TODO [ErdemBircan] mount react component
	}
}

/**
 * @module UpsellManager
 */
export default new UpsellManager();
